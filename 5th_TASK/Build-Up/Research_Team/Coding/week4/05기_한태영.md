## DCGAN 얼굴 생성 

메인 코드 레퍼런스: PyTorch 공식 튜토리얼: **DCGAN Tutorial**  
🔗 예시 레포: `https://github.com/yihong-chen/neural-collaborative-filtering`

- CelebA 얼굴 사진 데이터를 학습 후 **새로운 얼굴 이미지를 생성**하는 DCGAN
- **구성 요소**
  - Generator `G`: 노이즈 벡터 `z` → 3×64×64 RGB 얼굴 이미지 생성
  - Discriminator `D`: 3×64×64 이미지 → \"진짜/가짜\" 확률
  - 학습 루프: `D`와 `G`가 서로 경쟁하면서 동시에 학습

---

```python
# 데이터 루트
dataroot = "data/celeba"

workers = 2
batch_size = 128
image_size = 64 # 모든 이미지를 64X64로 맞춤
nc = 3 # 채널 수 
nz = 100 #노이즈의 차원
ngf = 64 # 생성자가 이미지를 만들어낼 떄 사용하는 기본 채널 수 ex.첫번쨰 층에서 입력이미지 -- ndf(64개) 채널로 특징 추출
ndf = 64 # 
num_epochs = 5
lr = 0.0002 #학습률
beta1 = 0.5
ngpu = 1

# 장치 선택
device = torch.device("cuda:0" if (torch.cuda.is_available() and ngpu > 0) else "cpu")
print("Device:", device)
```

---

```python
# 데이터 전처리 단계 -- 원본 사진들 크기 맞추기
dataset = dset.ImageFolder(
    root=dataroot,
    transform=transforms.Compose([
        transforms.Resize(image_size),
        transforms.CenterCrop(image_size),
        transforms.ToTensor(), # 픽셀값(0~255)을 0~1 사이의 값으로 정규화 해서 이미지 데이터 구조를 파이토치가 읽기 쉽게 바꾸기 위함함
        transforms.Normalize((0.5, 0.5, 0.5), (0.5, 0.5, 0.5)),  # 활성화 함수 tanh랑 범위 맞추기 위한 과정
    ]),
)

dataloader = torch.utils.data.DataLoader(
    dataset,
    batch_size=batch_size,   # 데이터 조금 씩 나누어주는 과정(batch)   // batch 를 128에서 16으로 바꾸면 시각화했을 때 나오는 사진 장수가 줄어든다
    shuffle=True,  #모델이 이미지 순서 외우지 못하게 하기 위함
    num_workers=workers,
)

# 샘플 이미지 시각화 
real_batch = next(iter(dataloader))
plt.figure(figsize=(6, 6))
plt.axis("off")
plt.title("Training Images (CelebA)")
plt.imshow(
    np.transpose(
        vutils.make_grid(real_batch[0][:64], padding=2, normalize=True).cpu(),
        (1, 2, 0),
    )
)
plt.show()
```

---

## Generator: z → 3×64×64 이미지

Upsampling

```python
class Generator(nn.Module):
    def __init__(self, ngpu: int):
        super().__init__()
        self.ngpu = ngpu
        self.main = nn.Sequential(
            # 입력 z: (batch, nz, 1, 1)
            nn.ConvTranspose2d(nz, ngf * 8, 4, 1, 0, bias=False),
            nn.BatchNorm2d(ngf * 8),
            nn.ReLU(True),  # (ngf*8) x 4 x 4

            nn.ConvTranspose2d(ngf * 8, ngf * 4, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ngf * 4),
            nn.ReLU(True),  # (ngf*4) x 8 x 8

            nn.ConvTranspose2d(ngf * 4, ngf * 2, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ngf * 2),
            nn.ReLU(True),  # (ngf*2) x 16 x 16

            nn.ConvTranspose2d(ngf * 2, ngf, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ngf),
            nn.ReLU(True),  # (ngf) x 32 x 32

            nn.ConvTranspose2d(ngf, nc, 4, 2, 1, bias=False),
            nn.Tanh(),  # (nc) x 64 x 64, 픽셀 범위 [-1, 1]   # 마지막 층을 Tanh. 데이터 전처리에서 이미지값 -1~1로 정규화 한 이유
        )

    def forward(self, input: torch.Tensor) -> torch.Tensor:
        return self.main(input)

#  nn.ConvTranspose2d: 이미지 크기로 계속 가로 2배, 세로 2배 키움. nn.BatchNorm2d: 데이터의 흐름 고르게 펴주는 다듬기 과정

# 생성기 생성 및 초기화
netG = Generator(ngpu).to(device)
if device.type == "cuda" and ngpu > 1:
    netG = nn.DataParallel(netG, list(range(ngpu)))

netG.apply(weights_init)
print(netG)
```

---

## Discriminator

```python
class Discriminator(nn.Module):
    def __init__(self, ngpu: int):
        super().__init__()
        self.ngpu = ngpu
        self.main = nn.Sequential(
            # 입력: (nc) x 64 x 64
            nn.Conv2d(nc, ndf, 4, 2, 1, bias=False),
            nn.LeakyReLU(0.2, inplace=True),  # (ndf) x 32 x 32

            nn.Conv2d(ndf, ndf * 2, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ndf * 2),
            nn.LeakyReLU(0.2, inplace=True),  # (ndf*2) x 16 x 16

            nn.Conv2d(ndf * 2, ndf * 4, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ndf * 4),
            nn.LeakyReLU(0.2, inplace=True),  # (ndf*4) x 8 x 8   #생성자랑 활성화 함수가 다름 -- D가 G에게 어디가 틀렸는지에 대한 힌트(Gradient)를 주기 위함

            nn.Conv2d(ndf * 4, ndf * 8, 4, 2, 1, bias=False),
            nn.BatchNorm2d(ndf * 8),
            nn.LeakyReLU(0.2, inplace=True),  # (ndf*8) x 4 x 4

            nn.Conv2d(ndf * 8, 1, 4, 1, 0, bias=False),
            nn.Sigmoid(),  # 출력: (batch, 1, 1, 1) → 진짜일 확률   #출력이 0(가짜) , 1(진짜) 로 나옴
        )

    def forward(self, input: torch.Tensor) -> torch.Tensor:
        return self.main(input)

# 커다란 이미지를 계속 줄여서 1x1로 만든다.

# 판별기 생성 및 초기화
netD = Discriminator(ngpu).to(device)
if device.type == "cuda" and ngpu > 1:
    netD = nn.DataParallel(netD, list(range(ngpu)))

netD.apply(weights_init)
print(netD)
```

---

## 손실 함수와 옵티마이저

```python
criterion = nn.BCELoss()  # 채점표

fixed_noise = torch.randn(64, nz, 1, 1, device=device)  # G의 발전 확인용

real_label = 1.0
fake_label = 0.0

optimizerD = optim.Adam(netD.parameters(), lr=lr, betas=(beta1, 0.999))
optimizerG = optim.Adam(netG.parameters(), lr=lr, betas=(beta1, 0.999))
```

---
##   흐름 
```python
img_list = []
G_losses = []
D_losses = []
iters = 0

print("Starting Training Loop...")
for epoch in range(num_epochs):
    for i, data in enumerate(dataloader, 0):
        ############################
        # (1) Update D: maximize log(D(x)) + log(1 - D(G(z)))
        ############################
        netD.zero_grad()

        # (1-1) real batch
        real_cpu = data[0].to(device)
        b_size = real_cpu.size(0)
        label = torch.full((b_size,), real_label, dtype=torch.float, device=device)
        output = netD(real_cpu).view(-1)
        errD_real = criterion(output, label)
        errD_real.backward()
        D_x = output.mean().item()

        # (1-2) fake batch
        noise = torch.randn(b_size, nz, 1, 1, device=device)
        fake = netG(noise)
        label.fill_(fake_label)
        output = netD(fake.detach()).view(-1)   #판별자 훈련시킬 때 생성자의 성능이 변하지 않게 하기 위해 detach()사용
        errD_fake = criterion(output, label)
        errD_fake.backward()
        D_G_z1 = output.mean().item()

        errD = errD_real + errD_fake
        optimizerD.step()

        ############################
        # (2) Update G: maximize log(D(G(z)))   생성자 훈련
        ############################
        netG.zero_grad()
        label.fill_(real_label)
        output = netD(fake).view(-1)
        errG = criterion(output, label)
        errG.backward()
        D_G_z2 = output.mean().item()
        optimizerG.step()

        if i % 50 == 0:
            print(
                f"[{epoch}/{num_epochs}][{i}/{len(dataloader)}]\tLoss_D: {errD.item():.4f}\t"
                f"Loss_G: {errG.item():.4f}\tD(x): {D_x:.4f}\tD(G(z)): {D_G_z1:.4f} / {D_G_z2:.4f}"
            )
# Loss_G . if) G가 만든 가짜를 보고 D가 0.99확률로 진짜라고 했다 . loss가 매우 작음 = D를 잘 속였다
#D(G(z)) = 가짜 이미지를 진짜라고 판단한 평균 확률 (0.5 수렴 - 이상적)
        G_losses.append(errG.item())
        D_losses.append(errD.item())

        if (iters % 500 == 0) or ((epoch == num_epochs - 1) and (i == len(dataloader) - 1)):
            with torch.no_grad():
                fake_fixed = netG(fixed_noise).detach().cpu()
            img_list.append(vutils.make_grid(fake_fixed, padding=2, normalize=True))

        iters += 1
```

